
management:
  health:
    pubsub:
      enabled: false
  metrics:
    export:
      stackdriver:
        project-id: sdc-rh-test
        enabled: false
        
spring:
  cloud:
    gcp:
      firestore:
        project-id: sdc-rh-test
      pubsub:
        enabled: false
        
